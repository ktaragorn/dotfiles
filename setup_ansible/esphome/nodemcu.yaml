substitutions:
  name: nodemcu
  platform: ESP8266
  board: nodemcu
  pin: D7
  inverted: 'false'

<<: !include base.yaml

binary_sensor:
  - <<: !include light_button.yaml

sensor:
  - platform: dht
    pin: D2
    temperature:
      name: "Bed Room Temperature"
    humidity:
      name: "Bed Room Humidity"
