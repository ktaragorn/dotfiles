<!--{{ansible_managed}} -->
<head>
    <style type="text/css">
        html{
            width: 100%;
            height: 100%;
        }
        body{
            background-image: url("https://source.unsplash.com/category/technology");
            background-size: cover;
        }
        div.service-body{
            display: flex;
            justify-content: center;
            align-items: center;
            height: 75%;
        }
        div.container{
            display: flex;
            flex-direction: row;
            padding: 10px;
            background-color: rgba(255,255,255,0.7);
            border-radius: 25px;
        }
        div.service{
       		padding: 20px;
//            background-color: rgba(255,255,255,0.7);
 //           border-radius: 25px;
        }
        .name{
        	display: flex;
            justify-content: center;
            font-size: 20pt;
        }
    </style>
</head>

<body>
<div class="service-body">
<div class="container">
    {% for service in proxied_services %}
    <div class="service">
    <a href="/{{service.name}}/">
    	<img src={{service.icon}} style="height:100px">
    	<div class="name">{{service.name |capitalize}}</div>
    <!-- {{(service.name| replace("_"," ")).split(" ")| capitalize | join(" ")}} -->
    </a>
    </div>
    {% endfor %}
</div>
</div>
</body>