d=~/dotfiles/bashrc.d

. $d/add_home_bin_path
. $d/add_this_bin_path
if [ -n "$BASH_VERSION" ]; then
   . $d/z.sh
   . $d/scm_ps1 #only for bash. This is already in zshrc as theme
fi

case "$-" in
*i*)    . $d/screenfetch ;;
esac

. $d/misc
